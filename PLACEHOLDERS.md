# Placeholders

- [ ] Hero Illustration (Serene Wellness Theme)
- [ ] User avatar photos (`users.avatar_url` is seeded `null`) — Dashboard falls back to `AvatarFallback` initials, no fabricated image
- [ ] Doctor profile photos (`doctors.photo_url` is seeded `null`) — Upcoming Bookings falls back to `AvatarFallback` initials, no fabricated image
- [ ] Expert profile photos (`experts.photo_url` is seeded `null`) — Expert Consult falls back to `AvatarFallback` initials, no fabricated image